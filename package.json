{
  "name": "focusboard",
  "version": "0.1.0",
  "description": "Personal task and workflow manager",
  "scripts": {
    "build": "esbuild src/js/render.js --bundle --outfile=dist/bundle.js",
    "watch": "esbuild src/js/render.js --bundle --outfile=dist/bundle.js --watch",
    "dev": "npx serve . --listen 3000",
    "build:dev": "npm run build && npm run dev"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "serve": "^14.0.0"
  }
}
